using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerTools : MonoBehaviour
{
    /*public Tools tool;
    public Tools otherTool1, otherTool2;
    private bool isPlayerInCollider;

    public void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.CompareTag("Player"))
        {
            Debug.Log("entrou no trigger");

            isPlayerInCollider = true;
        }
    }

    void OnTriggerExit2D(Collider2D collision)
    {
        if (collision.CompareTag("Player"))
        {
            Debug.Log("saiu do trigger");

            isPlayerInCollider = false;
        }
    }
    // Start is called before the first frame update
    void Start()
    {
        tool.isHolding = false;
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space) && isPlayerInCollider)
        {
            Debug.Log("Pegou foice");
            tool.isHolding = true; //ferramenta em quest�o � segurada pelo player

            otherTool1.isHolding = false; //Outras ferramentas s�o dropadas
            otherTool2.isHolding = false;
        }
    }*/
}
